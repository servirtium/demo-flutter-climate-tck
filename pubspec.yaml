name: servertium_flutter
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  test: any
  servirtium:
    git:
      url: https://github.com/servirtium/servirtium-dart.git
      path: servirtium

flutter:
  uses-material-design: true